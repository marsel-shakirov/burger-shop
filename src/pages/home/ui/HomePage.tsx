import { Container } from '@/shared/ui/container';
import { Catalog } from '@/widgets/catalog';

export const HomePage = () => {
  return (
    <main className="">
      <Container>
        <Catalog />
      </Container>
    </main>
  );
};
